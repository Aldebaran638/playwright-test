1. 功能整体描述:接下来我要写一系列模块，目的是从公司详情页-》经营风险板块中抓取以上信息。tyc/mid.html文件中有示例页面代码,而进入详情页后,需要找到并点击文本内容为"经营风险"的按钮后才能执行接下来的一系列操作.参考代码: page5.get_by_role("link", name="经营风险 35").click()但是这里的35是不固定的,所以实际代码中不要出现35

我的模块功能1：首先根据id="JS_tag_nav"，抓取类似 mid.html 2-41

的元素,然后将中间每一个"非纯数字的文本"转换为元素

接下来在下方有这一系列元素的具体内容.

模块功能2:观察到mid.html下方详情板块,大部分为表格内容,且表格中都有至少一个字段包含"日期"的字段,且表格迷人排序方式也是从上到下按照日期从晚到早排序的.
我期望实现一个模块:模块输入为两个日期(第二个日期必须晚于第一个),以及板块的名称(比如:股权出质,担保风险等.总之这个元素是模块1中得到的元素),输出就为详情板块中包含"日期"字段的数值,处在用户输入的两个日期之间的元素.举个例子,假设我输入2023-1-1  2026-1-1,那么 股权出质 板块中的第一行
```
1 530100202409060002 昆明市高速公路建设开发股份有限公司 昆明市城市综合交通枢纽有限责任公司 北京银行股份有限公司 2155.0万元 有效 2024-09-06 详情
```
将会被抓取,下面的一些在2024年的也会被抓取.
如果表格中第一行内容的日期早于用户输入的那个较早的日期,那么这一行剩下的都可以不用看了.

模块功能3:输入为模块功能1中得到的元素之一,如果一个板块中间同时出现VIP,支付,¥的字样(比如"担保风险"板块),那么就输出日志,说当前账号会员等级无法获知当前板块,需要跳过,并返回true.否则返回false

模块功能4:从公司详情页,点击经营风险板块标签.参考代码: page5.get_by_role("link", name="经营风险 35").click()但是这里的35是不固定的,所以实际代码中不要出现35

你先在tyc文件夹下创建一个新的文件夹,文件夹名字由你来定.这个文件夹中存储上述功能的一系列模块.其他的内容统统不要写

2. 
编写一个新模块，在business_risk下。
模块功能1：输入一个page，根据id="JS_tag_nav"，抓取类似 mid.html 2-41，的元素,然后将中间每一个"非纯数字的文本"（比如说注销备案，简易注销~公示催告等）转换为文本元素，将所有元素以及其后面的数字组成一个二元数组存储起来。返回这个二元数组


3. 
编写一个新模块，在business_risk下。
模块功能2:观察到mid.html下方详情板块,大部分为表格内容,且表格中都有至少一个字段包含"日期"的字段,且表格元素排序方式也是从上到下按照日期从晚到早排序的.
我期望实现一个新模块:模块输入为两个日期(第二个日期必须晚于第一个),以及板块的名称(比如:股权出质,担保风险等.总之这个元素是模块1中得到的元素),输出就为输入的板块名称对应的详情板块中包含"日期"字段的数值,处在用户输入的两个日期之间的元素.举个例子,假设我输入2023-1-1  2026-1-1,那么 股权出质 板块中的第一行
```
1 530100202409060002 昆明市高速公路建设开发股份有限公司 昆明市城市综合交通枢纽有限责任公司 北京银行股份有限公司 2155.0万元 有效 2024-09-06 详情
```
将会被抓取,下面的一些在2024年间的元素也会被抓取.
如果表格中第一行内容的日期早于用户输入的那个较早的日期,那么这一行剩下的都可以不用看了.

如果说没有找到输入板块名称对应的板块，那么输出调试日志,说没有找到对应的板块,并返回空列表.
如果说找到了输入板块名称对应的板块，但是没有找到包含"日期"字段的数值,那么输出调试日志,说没有找到对应的日期字段,并返回空列表.
如果说找到了输入板块名称对应的板块，且找到了包含"日期"字段的数值,但是没有内容符合要求，那么就正常返回空列表，并输出调试日志,说没有找到符合要求的日期.
如果说找到了输入板块名称对应的板块，且找到了包含"日期"字段的数值,且内容符合要求（不为空），那么就正常返回符合要求的日期列表

4. 
| 内容 | 业务标签 |
|------|------|
| 注销备案 | 0 |
| 简易注销 | 0 |
| 清算信息 | 0 |
| 惩戒名单 | 0 |
| 严重违法 | 0 |
| 行政处罚 | 8 |
| 环保处罚 | 0 |
| 税收违法 | 0 |
| 税务非正常户 | 0 |
| 欠税公告 | 0 |
| 违规处理 | 4 |
| 经营异常 | 0 |
| 政府约谈 | 0 |
| 产品召回 | 0 |
| 土地抵押 | 0 |
| 股权出质 | 11 |
| 股权质押 | 8 |
| 对外担保 | 2 |
| 担保风险 | 1 |
| 动产抵押 | 0 |
| 劳动仲裁 | 0 |
| 公示催告 | 1 |